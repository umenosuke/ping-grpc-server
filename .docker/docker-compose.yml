version: '3'
services:
  proto_build-image:
    build: proto_build
    tty: true
    user: "${_USER}"
    privileged: true
    container_name: proto_build
    volumes:
      - "../proto/:/target_data"
